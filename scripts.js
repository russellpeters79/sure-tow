document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('contactButton');
    button.addEventListener('click', () => {
        alert('call 07512 093921');
    });
});
